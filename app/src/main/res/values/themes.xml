<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AIASolutions" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.TuApp.Splash" parent="Theme.SplashScreen">

        <!-- Fondo del splash -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Icono que se muestra -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- Tema después del splash -->
        <item name="postSplashScreenTheme">@style/Theme.AIASolutions</item>

    </style>
    <style name="Theme.AIASolutions" parent="Base.Theme.AIASolutions" />
</resources>